# Architecture

Placeholder — write this up once Task #52 (Sheets spike) and the repository
interface pattern (Task #54) actually exist. Should cover: repository-interface
pattern, Sheets schema conventions.

Sheet schema conventions (tabs as tables, stable row IDs, header contract)
are written up already — see [sheet-schema.md](sheet-schema.md) (Task #53).
